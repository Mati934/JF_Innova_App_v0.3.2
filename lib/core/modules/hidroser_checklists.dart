import 'package:flutter/material.dart';

/// Colores de marca del módulo Hidroser. Se mantienen como constantes para
/// que tanto el tile del Home como las pantallas internas usen la misma paleta.
///
/// Nota: el módulo Hidroser dejó de ser un wrapper del Registro de Visita;
/// ahora vive en `lib/features/hidroser/` con sus propias tablas, PDF y
/// pantallas. Por eso aquí ya no se exportan tipos de checklist.
const Color kHidroserColor = Color(0xFF0277BD);
const Color kHidroserColorDark = Color(0xFF01579B);
